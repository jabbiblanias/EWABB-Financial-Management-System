{
  "buildCommand": "pip install -r requirements.txt && chmod +x build_files.sh && ./build_files.sh && python manage.py collectstatic --noinput",
  "outputDirectory": "staticfiles",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
